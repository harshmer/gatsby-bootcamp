/* import {graphql, useStaticQuery } from 'gatsby';

const data = useStaticQuery(graphql`
			query {
			  site {
			    siteMetadata {
			      title
			    }
			  }
			}
		`)

const footerData = useStaticQuery(graphql`
			query {
			  site {
			    siteMetadata {
			      author
			    }
			  }
			}
		`)

export default {footerData, data} */